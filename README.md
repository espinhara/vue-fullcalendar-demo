# vue-fullcalendar-demo
use of the store to save the annotations and link with the fullcalendar component, no backend will be used only vuex and vuetify

# run `yarn istall` 
get packeges

# run `yarn dev`
start development server

# run `yarn build`
build for production

