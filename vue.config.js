const path = require('path')

const webpack = require('webpack')

module.exports ={
    transpileDependencies: [
        'vuetify',
        // 'vue2-dropzone',
        // 'vue-echarts',
        // 'resize-detector'
      ],
} 